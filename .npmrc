# npm 12 blocks git dependencies by default; root allows only the ones this package.json declares (pi-codex-subagents PR branch)
allow-git=root

# pi provides the @earendil-works/* and typebox peers at runtime and itself installs with --legacy-peer-deps
legacy-peer-deps=true
